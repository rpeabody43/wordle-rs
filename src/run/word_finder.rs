use std::collections::HashMap;
use crate::run::dictionary;
use crate::run::wordle::match_wordle;

pub fn get_word (letters: &HashMap<char, (usize, bool)>, exact_arr: [&str; 5], dict: dictionary::Dictionary) {
    //TODO properly implement get_word
    println!("balls")
}

